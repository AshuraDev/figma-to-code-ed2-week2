import localFont from "next/font/local";

export const archivo = localFont({
  src: "../../public/fonts/Archivo.ttf",
  display: "swap",
});

export const chillax = localFont({
  src: "../../public/fonts/Chillax.ttf",
  display: "swap",
});
